# Rehrig Vision Pulse

Operations workspace for Rehrig Pacific and service providers.

The runnable app lives in [`vision-app/`](vision-app/README.md).

```bash
cd vision-app
npm install
npm run dev
```

Demo password is `vision`. Open http://localhost:5173 and pick a seed user on the login screen.
